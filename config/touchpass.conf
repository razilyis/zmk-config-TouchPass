# Enable TouchPass Module
CONFIG_ZMK_TOUCHPASS=n
CONFIG_ZMK_TOUCHPASS_ALWAYS_ON=n
CONFIG_ZMK_TOUCHPASS_SERIAL_RPC=n

# Enable USB CDC ACM for the config.html serial bridge
CONFIG_USB_DEVICE_STACK=y
CONFIG_UART_LINE_CTRL=y
CONFIG_USB_CDC_ACM=y

# Enable UART for Fingerprint Sensor
CONFIG_SERIAL=y
CONFIG_JSON_LIBRARY=y

# File System Requirements for Storage
CONFIG_FLASH=y
CONFIG_FLASH_MAP=y
CONFIG_FLASH_PAGE_LAYOUT=y
CONFIG_FILE_SYSTEM=y
CONFIG_FILE_SYSTEM_LITTLEFS=y
CONFIG_FS_LITTLEFS_FMP_DEV=y
CONFIG_FS_LITTLEFS_NUM_FILES=10
CONFIG_FS_LITTLEFS_NUM_DIRS=5

# USB HID for Keyboard
CONFIG_ZMK_USB=y
CONFIG_ZMK_BLE=y
# Keyboard Name
CONFIG_ZMK_KEYBOARD_NAME="TouchPass"

# Enable virtual kscan for standalone behavior
CONFIG_ZMK_KSCAN=y

# Use Internal RC Oscillator for LF Clock (More stable than XTAL if crystal issues exist)
CONFIG_CLOCK_CONTROL_NRF_K32SRC_RC=y
CONFIG_BOARD_ENABLE_DCDC=y

# Enable logging via USB
CONFIG_LOG=y
CONFIG_ZMK_LOG_LEVEL_INF=y
CONFIG_LOG_BUFFER_SIZE=2048
CONFIG_ZMK_USB_LOGGING=y

# ZMK_USB_LOGGING が select する値を明示的に設定（Kconfig warning 回避）
CONFIG_UART_INTERRUPT_DRIVEN=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# Fix for ZMK v0.3 settings reset compilation error
CONFIG_SETTINGS=y
CONFIG_SETTINGS_FILE=y
